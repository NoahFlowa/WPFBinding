<Window x:Class="WPF_Binding.Collection"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Binding"
        mc:Ignorable="d"
        Title="Collection" Height="300" Width="300">
    <Grid>
        <ListBox x:Name="_list" DisplayMemberPath="Name">

        </ListBox>
        <Button Content="Add" Height="Auto" Width="Auto" Click="OnAdd_Click" Grid.Row="1"/>
        <Button Content="Close" Height="Auto" Width="Auto" Click="close_Click" Margin="0,205,0,10"/>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
    </Grid>
</Window>
